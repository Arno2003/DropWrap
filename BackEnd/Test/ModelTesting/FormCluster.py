import pandas as pd
from sklearn.cluster import AgglomerativeClustering
import seaborn as sns

class FromCluster:
    def casteWiseCluster(df, parameters, priorityNum): # df -> dataframe, parameters -> 
       return  